using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestAnimText : MonoBehaviour
{
    Animation textAnim;
    // Start is called before the first frame update
    void Start()
    {
        textAnim = gameObject.GetComponent<Animation>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
